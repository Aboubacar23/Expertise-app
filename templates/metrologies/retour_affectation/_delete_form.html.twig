<form method="post" action="{{ path('app_retour_affectation_delete', {'id': retour_affectation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ retour_affectation.id) }}">
    <button class="btn">Delete</button>
</form>
