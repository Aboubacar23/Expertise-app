<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bon Intervention</title>
</head>
<style>
table{
    width: 100%;
}
.table { 
    width: 100%; 
    border-collapse: collapse; 
    }    
.tr:nth-of-type(odd) { 
    background: #eee; 
    }

.th { 
    background: #0063AF; 
    color: white; 
    font-weight: bold; 
    text-align: center; 
}

.td, .th { 
    padding: 10px; 
    border: 1px solid #0063AF; 
    font-size: 12px;
    }
td{
    padding: 10px;
}
.p{
    border-bottom: 2px solid #0063AF;
}
h2{
    text-align: center;
    text-transform: uppercase;
    font-size: x-large;
    font-family: serif;
    color: #0063AF
}
</style>
<body>
    <h2>Bon de sortie da n° {{ intervention.numeroDa}}</h2>
    <p class="p"></p>
    <div class="header">
        <table>
            <tr>
                <th>Prestataire : </th>
                <td>{{ intervention.prestataire }}</td>
                <th>Demandeur : </th>
                <td>{{ intervention.demandeur }}</td>
            </tr>
            <tr>
                <th>Date de DA : </th>
                <td>{{ intervention.dateDa ? intervention.dateDa|date('d-m-Y') : '' }}</td>
                <th>Date d'envoi : </th>
                <td>{{ intervention.dateEnvoi ? intervention.dateEnvoi|date('d-m-Y') : '' }}</td>
            </tr>
        </table>
    </div>
    <p class="p"></p>
    <div class="body">
        <table class="table">
            <thead>
                <tr  class="tr">
                    <th class="th" width="10%" style="width: 100px;">Repère</th>
                    <th class="th" width="22%" style="width: 100px;">Désignation</th>
                    <th class="th" width="16%" style="width: 100px;">Marque</th>
                    <th class="th" width="16%" style="width: 100px;">Type Intervention</th>
                    <th class="th" width="16%" style="width: 60px;">Date Retour prevue</th>
                    <th class="th" width="16%">Observation</th>
                </tr>
            </thead>
            <tbody>
                {% for item in intervention.linterventions %}
                    <tr> 
                        <td class="td">{{item.appareil}}</td>
                        <td class="td">{{item.designation}}</td>
                        <td class="td">{{item.marque}}</td>
                        <td class="td">{{item.typeIntervention}}</td>
                        <td class="td">{{item.dateRetour ? item.dateRetour|date('d-m-Y') : '' }}</td>
                        <td class="td">{{item.observation}}</td>
                    </tr>                                        
                {% endfor %} 
            </tbody>
        </table>
    </div>
</body>
</html>