<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jeumont Electric Group</title>
    <link rel="icon" href="{{asset('images/logo.png')}}" type="image/ico" />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" type="text/css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css" type="text/css">
    <link rel="stylesheet" href="{{asset('login/style.css')}}">
</head>
<body>
    <div class="container-fluid">
        <div class="row justify-content-center">
            <div class="col-12 col-sm-10 col-md-12 col-lg-11 col-xl-10">
                <div class="card d-flex mx-auto my-5">
                    <div class="row">
                        <div class="col-md-5 col-sm-12 col-xs-12 c1 p-5"> 
                            <img src="{{asset('images/logo.png')}}" width="200px" height="200px" class="mx-auto d-flex" alt="Teacher">
                            <div class="row justify-content-center">
                                <div class="w-75 mx-md-5 mx-1 mx-sm-2 mb-5 mt-4 px-sm-5 px-md-2 px-xl-1 px-2">
                                    <h1 class="wlcm">Bienvenue sur la Base d'Expertise</h1> 
                                    <span class="sp1"> <span class="px-3 bg-danger rounded-pill"></span> 
                                    <span class="ml-2 px-1 rounded-circle"></span> 
                                    <span class="ml-2 px-1 rounded-circle"></span>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-7 col-sm-12 col-xs-12 c2 px-5 pt-5">
                            <form class="px-5 pb-5" method="POST">
                                <div class="d-flex"> 
                                    <h3 class="font-weight-bold">Se Connecter</h3>
                                </div> 
                                {% if error %}
                                    <div class="alert alert-danger">Nom d'utilisateur ou mot de passe introuvable!</div>
                                {% endif %}

                                {% if app.user %}
                                    <div class="mb-3">
                                        Vous êtes connecté en tant que {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}" class="text-primary">Déconnexion</a>
                                    </div>
                                {% endif %}
                                <input type="text" value="{{ last_username }}" name="username" id="inputUsername" class="form-control m-2" autocomplete="username" required autofocus placeholder="Nom utilisateur">
                                <input type="password" name="password" id="inputPassword" class="form-control m-2" autocomplete="current-password" required placeholder="Mot de passe">
                                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                                <button class="text-white text-weight-bold bt m-2" >Connexion</button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
</html>
